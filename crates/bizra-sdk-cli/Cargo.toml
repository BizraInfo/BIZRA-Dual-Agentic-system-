[package]
name = "bizra-sdk-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
bizra-sdk-core = { path = "../bizra-sdk-core", features = ["ollama", "memory"] }
clap = { version = "4.5", features = ["derive"] }

anyhow = "1.0"
serde_json = "1.0"
tokio = { version = "1.0", features = ["full"] }
